/*
-array is a linear collection of similar datatype elements.
-array is a group pof variable.

  array=   a[5] this is array of 5 variable



vid-1



#include<stdio.h>
#include<conio.h>
main()
{
 int a[10],i,sum=0;
 float avg;
 clrscr();
 printf("enter 10 numbers");
 for(i=0;i<=9;i++)
	scanf("%d",&a[i]);
 for(i=0;i<=9;i++)
 sum=sum+a[i];
 avg=sum/10.0;
 printf("avg=%f",avg);
 getch();
}




vid2

int a[]; error
int a[5]; {9,6,8,0,2} this consumes 10 bytes
int a[]; {9,6,8,0,2} if initialize at the time of declaration we can leave empty index
int a[5]; {9,6,8,0,2,5,7} this shows error we can't assign more value than its size
int a[5]; {9,6,8,0} we can assign less value and garbage value in empty garbage value=0
		a
	   0 1 2 3 4 //this is index of array always start with 0
	   9 6 8 0 2 //



//program of largest numbers

#include<stdio.h>
#include<conio.h>
main()
{
 int a[5],max,i;
 clrscr();
 printf("enter five numbers");
 for(i=0;i<=4;i++)
	scanf("%d",&a[i]);
 max=a[0];
 for(i=0;i<=4;i++)
 {
	if(a[i]<max)
	max=a[i];
 }
 printf("smallest number is =%d",max);
 getch();
}

//program of odd and even numbers in array

#include<stdio.h>
#include<conio.h>
main()
{
 int a[10],i,even=0,odd=0;
 clrscr();
 printf("enter 10 numbers");
 for(i=0;i<=9;i++)
	scanf("%d",&a[i]);
 for(i=0;i<=9;i++)
 {
	if(a[i]%2==0)
	even++;
	else
	odd++;
 }
 printf("\neven=%d,odd=%d",even,odd);
 getch();
}

decalration at the time of initialization-



#include<stdio.h>
#include<conio.h>
main()
{
 int sum,a[5]={2,5,3,3,3};
 clrscr();
 sum=a[0]+a[1]+a[2]+a[3]+a[4];
 printf("sum=%d",sum);
 getch();
}

	*/

#include<stdio.h>
#include<conio.h>
main()
{
 int
 clrscr();
 printf("");
 getch();
}