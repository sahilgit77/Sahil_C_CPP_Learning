/* DECISION
 CONTROL IN C
   conditional operator(?:)-it is also a ternery operator

  if(condition)
  {
  line 1;
  line 2;
  ....
  }
  else
  {
  line 1;
  line 2;
  ....
  }

						       */


      /*  1.

       #include<stdio.h>
       #include<conio.h>
       main()
       {
       int x;
       clrscr();
       printf("enter a number");
       scanf("%d",&x);
       x>0 ? printf("positive") : printf("negative");     //if condn is true then stmt in middle will be print eg. printf("positive") and if condn will be false then last stmt will be print here printf("negative");
       getch();
       }

	*/
	/*  2.

	#include<stdio.h>
	#include<conio.h>
	main()
	{
	int x,y,max;
	clrscr();
	printf("enter both numbers");
	scanf("%d\n %d",&x,&y);
	max=x>y ? x : y;
	printf("greater number is %d",max);
	getch();
	}                                  */

	//3. without max same programmme


	#include<stdio.h>
	#include<conio.h>
	main()
	{
	int x,y;
	clrscr();
	printf("enter both numbers");
	scanf("%d\n%d",&x,&y);
	printf("greater number is %d",x>y?x:y);
	getch();
	}




 /*
  * use of selective assignment
     - x=a>b?a:b;  if condn will true then middle stmt will be print
		   if false then last stmt will be print. i.e. selected option
		   will be stored in 'x'.



	*/